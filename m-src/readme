This directory contains artefacts for the mobile demo.  They are processed in
various ways by ../build and the result is ultimately put in ../m/

../build also uses some files from ../web-src to avoid repetition.
