Examples are prefixed with a number, so they can sort properly in scripts that process them.

The expected output of X.apl should be in X.out; running ./test ensures that all examples produce their expected outputs.
